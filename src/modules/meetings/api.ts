// modules/meetings/api.ts
// export async function getMeetings({ page }: { page: number }) {
//     const res = await fetch(`${process.env.NEXT_PUBLIC_API_URL}/meetings?page=${page}`, {
//       cache: "no-store", // ensures fresh data
//     });
  
//     if (!res.ok) throw new Error("Failed to fetch meetings");
//     return res.json();
//   }
  
  // error testing page